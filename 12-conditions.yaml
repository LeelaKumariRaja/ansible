- name: conditions
  hosts: localhost
  tasks: 
  - name: check user
    ansible.builtin.command: id expense
    register: USER
    ignore_errors: true
  
  - name: print user
    ansible.builtin.debug:
      msg: "userinfo is {{USER}}"

  - name: say hello
    ansible.builtin.debug:
      msg: "hello"